@using DP001BusinessLogic.ViewModels;
@model PriceRuleViewModel

<div id="bandsInfoContainer" class="container text-center"></div>

@*<section class="container-fluid">
    <div class="row">
        <div class="col-xs-12">*@
            <section class="list-form table-responsive">
                <table id="banding-table" class="table table-hover @(!Model.PriceRuleEntry.IsBanding ? "hidden" : "")">
                    <thead>
                        <tr>
                            <th>
                                Banding Description
                            </th>
                            <th>
                                Band Start
                            </th>
                            <th>
                                Band End
                            </th>
                            <th>
                                Product Count
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var band in Model.BandList)
                        {
                            Html.RenderPartial("BandRow", band);
                        }
                    </tbody>
                    <tfoot>
                        <tr class="list-form-row" data-newurl="@(Url.Action("NewBand"))">
                            <td colspan="100%">
                                <button class="g-m-tb-5 g-cur-p btn btn-default btn-sm btn-add">
                                    <i class="fa fa-plus"></i> Add Band
                                </button>
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </section>
        @*</div>
    </div>
</section>*@
