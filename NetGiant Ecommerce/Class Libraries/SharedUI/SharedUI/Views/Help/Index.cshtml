@using BusinessLogic.ViewModels;

@model CommonViewModel

<div class="g-bc-p g-p-t-60 g-p-b-60">
    <div class="container body-content g-ps-r">
        <div class="row g-m-t-20 g-m-b-40">
            <div class="col-md-12">
                <iframe id="help" class="help" src="about:blank">
                    @*@Html.Raw(ViewBag.PageContent.ToString())*@
                </iframe>
            </div>
        </div>
    </div>
</div>

<div class="hidden" id="frameSource">
    @*@Html.Raw(ViewBag.PageContent.ToString())*@
</div>

