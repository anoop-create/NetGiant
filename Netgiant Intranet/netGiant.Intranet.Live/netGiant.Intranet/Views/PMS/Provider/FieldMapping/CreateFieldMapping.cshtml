@model netGiant.Intranet.BusinessLayer.ViewModels.PMS.Provider.ProviderViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Field Mapping";
    ViewBag.SubTitle = Model.fieldMapp.fieldMappingID > 0 ? "Edit" : "Create";
}

<div class="placeHolder">
    <div class="placeHolderBody">

        <h4>@ViewBag.SubTitle @ViewBag.Title</h4>

        <div class="form-horizontal">

            @using (Html.BeginForm("SaveFieldMapping", "Provider", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(m => m.fieldMapp.fieldMappingID)
                @Html.HiddenFor(m => m.selectedProviderID)

                <div class="form-group">
                    @Html.Label("Provider", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.DropDownListFor(m => m.fieldMapp.providerFK, Model.allProviders, "Select...", new { @class = "form-control g-largeCell" })
                        @Html.ValidationMessageFor(model => model.fieldMapp.providerFK, "", new { @class = "text-danger" })
                    </div>
                </div>

                @*<div class="form-group">
                    @Html.Label("PMS Field", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.fieldMapp.fieldMappingTo, new { htmlAttributes = new { @class = "form-control g-largeCell" } })
                        @Html.ValidationMessageFor(model => model.fieldMapp.fieldMappingTo, "", new { @class = "text-danger" })
                    </div>
                </div>*@

                <div class="form-group">
                    @Html.Label("PMS Field", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.DropDownListFor(m => m.fieldMapp.fieldMappingTo, Model.allPMSMappingFields, "Select...", new { @class = "form-control g-largeCell" })
                        @Html.ValidationMessageFor(model => model.fieldMapp.fieldMappingTo, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("Provider's Field", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @if (Model.hasColumnHeaders)
                        {
                            @Html.EditorFor(model => model.fieldMapp.fieldMappingWith, new { htmlAttributes = new { @class = "form-control g-largeCell" } })
                        }
                        else
                        {
                            @Html.EditorFor(model => model.fieldMapp.fieldMappingWith, new { htmlAttributes = new { @class = "form-control g-largeCell", onkeydown = "return ValidateNumber(event);", @autocomplete="off" } })
                            <span><b>Note:</b>Please provide only column index (Numbers only).</span><br />
                        }
                        @Html.ValidationMessageFor(model => model.fieldMapp.fieldMappingWith, "", new { @class = "text-danger" })
                    </div>
                </div>
                
                if (Model.fieldMapp.fieldMappingID > 0)
                {
                    <div class="form-group">
                        @Html.Label("Date Last Updated", htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.EditorFor(model => model.fieldMapp.dateLastUpdate, new { htmlAttributes = new { @class = "form-control g-largeCell", @disabled = "disabled" } })
                        </div>
                    </div>
                }

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" name="saveFieldMapping" value="Save" class="btn btn-default" title="Save" />
                        @if (Model.selectedProviderID > 0)
                        {
                            @Html.ActionLink("Cancel", "ProviderDetails", "Provider", new { id = Model.selectedProviderID }, new { @class = "btn btn-default" })
                        }
                        else
                        {
                            @Html.ActionLink("Cancel", "FieldMapping", "Provider", null, new { @class = "btn btn-default" })
                        }
                    </div>
                </div>
            }

        </div>

    </div>
</div>

@section Scripts {
    <script type="text/javascript">

        function ValidateNumber(e) {
            var evt = (e) ? e : window.event;
            var charCode = (evt.keyCode) ? evt.keyCode : evt.which;
            if (charCode > 31 && (charCode < 48 || charCode > 57)) {
                return false;
            }
            return true;
        };

    </script>
 }