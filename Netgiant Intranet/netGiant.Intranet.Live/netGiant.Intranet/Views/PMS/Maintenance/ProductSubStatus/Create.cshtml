@*@model netGiant.Intranet.DataLayer.productSubStatus

@{
    ViewBag.Title = "Product Sub Status";
    ViewBag.SubTitle = Model.productSubStatusID > 0 ? "Edit" : "Create";
}

<div class="placeHolderHeading gl_textType2b">Product Management System &rarr; @ViewBag.Title &rarr; @ViewBag.SubTitle<hr /></div>
<div class="placeHolderBody">

    <h3>@ViewBag.SubTitle @ViewBag.Title</h3>

    @Html.Hidden("SaveProductSubStatusURL", Url.Action("Save", "ProductSubStatus"))

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(m => m.productSubStatusID)

        <div class="form-group">
            @Html.Label("Product Sub Status Name", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.productSubStatusName, new { htmlAttributes = new { @class = "form-control g-largeCell" } })
                @Html.ValidationMessageFor(model => model.productSubStatusName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a id="lnkProductSubStatusSave" href="#" class="btn btn-default">Save</a>
                <a id="lnkProductSubStatusCancel" href="#" class="btn btn-default">Cancel</a>
            </div>
        </div>
    </div>

</div>*@