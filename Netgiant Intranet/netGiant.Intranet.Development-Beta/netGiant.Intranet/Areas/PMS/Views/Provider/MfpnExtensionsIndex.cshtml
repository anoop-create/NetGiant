@using netGiant.Intranet.BusinessLayer.ViewModels.PMS.Provider
@model ProviderViewModel

@{
    ViewBag.Title = "MFPN Extensions";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="placeHolder g-m-b--20">
    <h4 class="text-center">@ViewBag.Title</h4>
</div>

@if (User.IsInRole("IntranetAdmin") || User.IsInRole("PMSAdmin"))
{
    @Html.ActionLink("Create New", "CreateMfpnExtension", "Provider", new { id = 0 }, new { @title = "Create new mfpn extension", @class = "g-p-10" })
}

@(Html.Kendo().Grid<TelerikMfpnExtension>()
                        .HtmlAttributes(new { @class = "pointer-rows" })
                        .Name("MfpnExtensionGrid")
                        .Columns(columns =>
                        {
                            columns.Bound(x => x.Manufacturer).Title("Manufacturer");
                            columns.Bound(x => x.Extension).Title("Extension");
                            columns.Bound(x => x.Id).ClientTemplate(
                                    "<a href=\"CreateMfpnExtension/#:Id#\">Edit</a>" +
                                    "|<a href=\"##\" class=\"lnkDelete\" value=\"#:Id#\" title=\"Delete\">Delete</a>"
                                    ).Width("136px").Filterable(false).Sortable(false).Groupable(false).Title("");
                        })
                        .Pageable(pager => pager.Refresh(true))
                        .Pageable(pager => pager.PageSizes(new List<int> { 150, 100, 50, 20 }))
                        .Sortable(sortable => sortable.AllowUnsort(true))
                        .Groupable()
                        .Scrollable()
                        .Filterable(filter => filter.Mode(GridFilterMode.Menu))
                        .ColumnMenu()
                        .Resizable(resize => resize.Columns(true))
                        .Reorderable(reorder => reorder.Columns(true))
                        .NoRecords("There are no mfpn extensions to display")
                        .DataSource(dataSource => dataSource
                            .Ajax()
                            .Read(x => x.Action("MfpnExtensions_Read", "Provider"))
                            .PageSize(200)
                        )
                        .Events(e => e.DataBound("saveGridFilters"))
)

<script type="text/javascript">
    $(function () {
        $(document).on('click', '.lnkDelete', function (e) {
            var rowid = $(this).attr('value');
            e.preventDefault();
            $.confirm({
                title: 'Mfpn Extensions',
                content: 'Are you sure you want to delete this mfpn extension?',
                buttons: {
                    Yes: function () {
                        deleteGridRow('@Url.Action("DeleteMfpnExtension", "Provider")/', { id: rowid });
                    },
                    No: function () { }
                }
            });
        });
    });
</script>