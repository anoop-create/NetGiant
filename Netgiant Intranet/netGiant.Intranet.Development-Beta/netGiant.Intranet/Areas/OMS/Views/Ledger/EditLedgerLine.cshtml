@using netGiant.Intranet.BusinessLayer.ViewModels.OMS;
@model LedgerViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.SubTitle = Model.LedgerLine.LedgerLineId > 0 ? "Edit" : "Create";
}

<div class="placeHolder">
    <div class="placeHolderBody">

        <h4>@ViewBag.SubTitle LedgerLine</h4>

        <div class="form-horizontal">
            @using (Html.BeginForm("SaveLedgerLine", "Ledger", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                @Html.HiddenFor(x => x.LedgerLine.LedgerLineId)

                <div class="LedgerLineTabs" style="background:none;">
                    <ul>
                        <li>
                            <a href="#LedgerLineDetails">LedgerLine Details</a>
                        </li>
                    </ul>
                </div>

                <div id="LedgerLineDetails">
                    <fieldset class="g-m-t-20">
                        <legend>Ledger Line Details</legend>

                        <div class="form-group">
                            @Html.Label("Description", htmlAttributes: new { @class = "control-label col-md-2" })
                            <div class="col-md-10">
                                @Html.EditorFor(model => model.LedgerLine.Description, new { htmlAttributes = new { @class = "form-control g-largeCell" } })
                                @Html.ValidationMessageFor(model => model.LedgerLine.Description, "", new { @class = "text-danger" })
                            </div>
                        </div>
                    </fieldset>

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" name="SaveLedgerLine" value="Save" class="btn btn-default" title="Save" />

                            @Html.ActionLink(
                            linkText: "Cancel",
                            actionName: "ViewLedgerLine",
                            controllerName: "Ledger",
                            routeValues: new { id = Model.LedgerLine.LedgerLineId },
                            htmlAttributes: new { @class = "btn btn-default" }
                            )
                        </div>
                    </div>
                </div>
             }
        </div>
    </div>
</div>

<script>

    $(document).ready(function () {
        $(".LedgerLineTabs").tabs();
    });

</script>
